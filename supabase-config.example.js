/*
 * 复制为 supabase-config.local.js 后填写。
 * Publishable / anon key 可以在启用 RLS 的前提下用于浏览器；绝不要填写 service_role key。
 */
window.SUPABASE_CONFIG = {
  url: "https://YOUR_PROJECT.supabase.co",
  anonKey: "YOUR_PUBLISHABLE_OR_ANON_KEY"
};
