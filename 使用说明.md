# 校招 Base 地图看板

## 打开方式

双击 `启动看板.command`。浏览器会自动打开看板；使用期间请保持启动窗口开启。

如果 macOS 第一次阻止打开，可在 Finder 中右键该文件并选择“打开”。

## 启用高德地点搜索

1. 用文本编辑器打开 `amap-config.local.js`。
2. 把高德控制台中的 **Web端（JS API）Key** 填到 `key` 后面的引号中。
3. 把同一应用的 **安全密钥（securityJsCode）** 填到 `securityJsCode` 后面的引号中。
4. 保存文件，重新打开看板。新增岗位窗口右上角显示“高德 POI”即表示配置生效。

示例只展示格式，请勿照抄：

```js
window.AMAP_CONFIG = {
  key: "你的 Web端（JS API）Key",
  securityJsCode: "你的安全密钥"
};
```

这两个值只需保存在你的电脑上，不要发到聊天、GitHub 或公开网页。若没有配置或高德临时加载失败，看板会自动使用开放地图搜索作为兜底。

## 位置功能

- 主视图以可旋转的 Sentinel-2 卫星 3D 地球展示岗位分布；选择岗位或继续放大后，会在同一张地图中自动渐变为清晰街道。
- 右侧数据舱会随进度、城市和偏好筛选实时更新，展示流程分布、城市机会与未来 7 天行动负荷。
- 新增岗位时可搜索公司、写字楼、园区、联合办公、商场或详细地址，再从多个 POI 候选中选择正确地点。
- 选择后可在预览地图中拖动 Pin 微调；拖动后会自动降级为“大致区域”，避免地址文字与坐标被误认为完全确认。
- 搜索不到时仍可直接在预览地图上点选，或暂存为“地址待确认”。
- 高德返回的 GCJ-02 坐标会转换为 WGS84 保存，以便和 3D 地球及街道底图正确对齐；原始坐标也会一并保留在备份数据中。

## 公共交通行程

- 选择任意岗位后点击底部信息卡的“查看行程”，再选择另一个岗位、朋友点位，或搜索一个任意地点作为终点。
- 起点和终点可以互换；两端都支持岗位、朋友点位和搜索地点，因此也可以比较“朋友 → 某地”或“两个办公地点之间”的通勤。
- 配置高德 Key 后会展示公共交通候选方案、总耗时、步行距离、换乘次数、票价与主要线路。选中方案后，地图会用虚线标出步行段，并用不同颜色区分每一段公交或地铁线路。
- 方案下方的“分段行程”会显示每段步行、上车站、下车站、换乘站、地铁出入口与途经站；同一换乘段存在多条等价公交线路时会明确标注“任选其一”。

## 朋友的校招

- 点击顶部“朋友的校招”，填写昵称并创建共享码；朋友输入共享码后即可订阅你的点位。
- 共享数据只包含不透明点位编号、城市和经纬度，不包含公司、岗位、进度、楼栋或详细地址文字。所有朋友点位都使用同一种粉色标记。
- 岗位地点有变化时会自动更新共享空间；朋友端每 60 秒同步一次，也可以手动刷新。
- 点击“停止共享”会让原共享码失效；朋友端下次同步时会自动移除你的点位。
- 登录云端账户后，共享空间与朋友订阅会保存到 Supabase，可跨设备使用；未配置云端时仍保留本机同步模式作为开发兜底。

## 账户与跨设备同步

- 配置 Supabase 后，首页会显示登录、创建账户和找回密码入口。
- 第一次登录时，当前浏览器已有的岗位记录会自动迁移到该账户；之后岗位与日程会保存到云端，并在浏览器保留一份按账户隔离的缓存。
- 退出账户后不会在界面中继续展示该账户的数据。不同账户之间由数据库 Row Level Security 强制隔离。
- `supabase-config.local.js` 只能填写 Publishable / anon key，绝不能填写 `service_role` key。

## 数据与备份

未登录时，岗位记录只保存在当前浏览器；登录后会同步到你的 Supabase 账户。只有在你主动创建共享码后，经过隐私裁剪的点位坐标才可被持有共享码的已登录朋友读取。仍建议定期使用右上角“备份”导出 JSON 文件。

3D 卫星地球、街道底图和地点搜索需要联网。配置后地点搜索优先使用高德 POI；卫星影像来自 EOX/Copernicus，街道数据来自 OpenStreetMap。选择后仍建议核对地址与 Pin。
